import { TxtNumberDirective } from './txt-number.directive';

describe('TxtNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new TxtNumberDirective();
    expect(directive).toBeTruthy();
  });
});
